{
  "name": "GitHubAgenticWorkflows Codespace",
  // ... 他の設定 ...

  "customizations": {
    "codespaces": {
      "repositories": {
        // 現在のリポジトリの権限を上書き
        "ymd65536/GitHubAgenticWorkflows": { 
          "permissions": {
            // ★この設定が重要です★
            // ワークフローの実行（dispatches）に必要な 'write' 権限を明示的に付与
            "actions": "write" 
          }
        }
      }
    }
  }
}